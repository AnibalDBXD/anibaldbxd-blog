import React from 'react';

interface IUser {
  title: React.ReactNode;
  pageTitle: string;
  mainTitle: string;
}

const user: IUser = {
  title: 'AnibalDBXD',
  mainTitle: 'Blog',
  pageTitle: ' | AnibalDBXD',
};

export default user;
