// eslint-disable-next-line no-shadow
enum PATHS {
  home = '/',
  post = '/[id]',
  more = '/more',
  // used in LayoutWrapper
  mockHome = '/mocks',
  mockPost = '/mocks/[mockId]',
  mockMore = '/mocks/more',
}

export default PATHS;
